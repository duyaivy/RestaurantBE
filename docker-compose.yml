services:
  web:
    build: .
    container_name: restaurant-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/app/restaurantBE/chatbot/rag/.chroma

volumes:
  chroma-data:
